# D08 PTW — Next Gate

PTW remains NOT PROMOTED until these implementation controls are closed:

1. C03 server identity/role/scope resolution is used by Convex, rather than caller-supplied scope alone.
2. Historical import has an executable dry-run/reconciliation path.
3. UI end-to-end scenario exercises authorized and unauthorized branch access.
4. GitHub PTW Runtime Gate is green on the resulting final commit.

Once all four are PASS, D08 becomes the reference implementation and the platform skeleton is ready for the next domain.
