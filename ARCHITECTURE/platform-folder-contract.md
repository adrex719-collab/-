# Platform Folder Contract

Canonical implementation surfaces:

- `platform/` — browser presentation/demo
- `runtime/` — runtime contracts and executable boundaries
- `convex/` — Macaly backend implementation where applicable
- `ARCHITECTURE/` — Target Architecture and contracts
- `TESTS/` — automated gates and regression controls

GitHub remains the technical Source of Truth. Macaly is not a second manually maintained implementation.
